import { FieldType } from "@/types"
import React from "react"

type Props = {
   field: FieldType
}

function FieldPreview(props: Props) {
   return <div className='col-span-1'>dsf</div>
}

export default FieldPreview
